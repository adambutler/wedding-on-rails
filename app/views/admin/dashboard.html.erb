<h1>Admin</h1>

<div class="metric">
  <%= @guests.count %>
  <small><%= "Guests".pluralize(@guests.count) %></small>
</div>

<h2>Guests</h2>

<ul>
  <% @guests.each do |guest| %>
    <li><%= guest %></li>
  <% end %>
</ul>
